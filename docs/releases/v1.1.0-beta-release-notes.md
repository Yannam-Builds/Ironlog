# IRONLOG 1.1.0-beta (Android)

Build target: commit `80cd6b7` (RC refresh)

## Included in this beta refresh

- Library + plan system hardening pass with canonical alias resolution and safer template validation.
- Smart notification engine closure in Balanced mode with deterministic caps, cooldowns, quiet-hours, and already-actioned suppression.
- Export/import continuity upgrades so notification/recovery/milestone state round-trips with SQLite v2 bundles.
- Home screen weekly summary alignment fixes and UI polish from latest device QA.
- Release-closure QA gate (`qa:rc`) wired and passing.

## Data and migration safety

- Existing 1.0/1.1 user logs remain preserved on in-place updates.
- Local-first storage remains default; uninstall continuity requires encrypted backup or export/import restore.
- Migration + export/import contract checks are included in the RC QA pass.

## APK

- `IRONLOG-v1.1.0-beta-rc.apk`
- Android only
